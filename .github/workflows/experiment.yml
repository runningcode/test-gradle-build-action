name: Experiment
on: 
  workflow_dispatch:

jobs:
  job1:
    name: Debug
    runs-on: windows-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v2
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
    - name: List files
      run: ls -la
    - name: List files with WSL
      run: wsl ls -la
    - name: What's home?
      run: echo $HOME
